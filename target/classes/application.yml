spring:
  application:
    name: lms

  datasource:
    url: jdbc:mysql://localhost:3306/LibraryDB
    username: simone_zheng
    password: Sasso123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true